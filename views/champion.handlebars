div class="container">
    <nav>
    <div class="navbar">
        <a href="/" class="header-brand">LeagueOL101</a>
        <ul>
     <li><a href="/champions">Back</a><img src="/static/back.png"></li>
        </ul>
    </div>
    </nav>
    </div>

    <div class="container">
    <div class="champion-title">
        <h1>{{ champion.name }}</h1>
    </div>
    <div class="section-container">
    <div class="ability-container">
    <div class="abilities">
        {{#each champion.abilities }}
        <img src="{{ icon }}">
        {{/each}}
    </div>
    </div>
    <div class="a-b-container">
    <div class="tabContainer">
    <div class="buttonContainer">
        <button onclick="showPanel(0,'#3C484D')">Tips</button>
        <button onclick="showPanel(1, '#3C484D')">Lore</button>
        <button onclick="showPanel(2,'#3C484D')">Items</button>
    </div>
    <div class="tabPanel">
        <h2>Difficulty: {{ champion.difficulty }}</h2>
    {{#each champion.tags }}
    <h3>Class: {{ this }}</h3>
    {{/each}}
    {{#each champion.tips.playingAs }}
    <h3>Playing As: {{ this }}</h3>
    {{/each}}
    {{#each champion.tips.playingAgainst }}
    <h3>Playing Against: {{ this }}</h3>
    {{/each}}</div>
    <div class="tabPanel"><h3>{{ champion.lore }}</h3></div>
    <div class="tabPanel">{{#each champion.recommended.[0] }}
    <h3>Block name: {{ this.type }}</h3>
    {{#if this.jg_exists}}
        <h3>HES A JUNGLER KEKW</h3>
    {{else}}
        <h3>RITO SAYS JG IS TROLL</h3>
    {{/if}}
    {{#each this.items }}
        <h3>Item name: {{ info.name }}</h3>
        {{!-- <h3>Item description: {{ info.description }}</h3> --}}
        {{!-- <h3>Mini tip: {{ info.text }}</h3>
        <h3>Total cost: {{ info.totalGold }}</h3> --}}
        <img src="{{ info.icon }}">
    {{/each}}
{{/each}}</div>
</div>
       
    </div>
    <div class="splash-art"> 
        
        <div class="splash">
            
     {{#each champion.skins }}
        {{#if @first}}
   {{!-- <h3>{{ skin_name }}</h3> --}}
    <img src="{{ splash_url }}">
    {{/if}}
    {{/each}}
    
    </div>
    <div class="name-tag">
        <h1>{{ champion.title }}</h1>
            </div>
    </div>  

   
    </div>
    </div>
    </div>
   
{{!--- note: Maybe put it in carousel format. }}

{{!-- <img src="{{ champion.icon }}"> --}}
{{!-- --}}




{{!-- Abilities --}}
{{!-- {{#each champion.abilities }}
    <h3>ID: {{ id }}</h3>
    <h3>Name: {{ name }}</h3>
    <h3>Cost Type: {{ costType }}</h3>
    <img src="{{ icon }}">
    <h3>Description: {{ description }}</h3>
    <h3>Cooldown Per Level String: {{ cooldownString }}</h3>
    <h3>Cooldown Array: {{ cooldown }}</h3>
    <h3>Damage Per Level String: {{ damageString }}</h3>
    <h3>Damage Array: {{ damage }}</h3>
    <h3>---------------------</h3>
{{/each}} --}}

{{!-- Skin names SplashArts LoadingScreen
{{#each champion.skins }}
    <h3>{{ skin_name }}</h3>
    <img src="{{ splash_url }}">
    <img src="{{ loadingScreen_url   }}">
{{/each}} --}}

<script>
var tabButtons=document.querySelectorAll(".tabContainer .buttonContainer button");
var tabPanels=document.querySelectorAll(".tabContainer  .tabPanel");

function showPanel(panelIndex,colorCode) {
    tabButtons.forEach(function(node){
        node.style.backgroundColor="";
        node.style.color="";
    });
    tabButtons[panelIndex].style.backgroundColor=colorCode;
    tabButtons[panelIndex].style.color="white";
    tabPanels.forEach(function(node){
        node.style.display="none";
    });
    tabPanels[panelIndex].style.display="block";
    tabPanels[panelIndex].style.backgroundColor=colorCode;
}
showPanel(0,'#3C484D');
</script>


